# .zprofile — runs for login shells, AFTER /etc/zprofile (which runs path_helper).
# path_helper rebuilds PATH from /etc/paths, wiping .zshenv additions.
# Re-source .zshenv to restore our PATH entries.

source "$HOME/.zshenv"
